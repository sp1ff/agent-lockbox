[Unit]
Description=Agent Lockbox notification listener

[Service]
Type=simple
ExecStart=%h/bin/agent-lockbox-notify-listener

# Ensure logs go to the user journal
StandardOutput=journal
StandardError=journal

# Optional but commonly useful
Restart=on-failure
RestartSec=2

[Install]
WantedBy=default.target
